# Archive

`legacy-2018/` is the original bash + vim setup (forked from justjkk/dotfiles,
last touched 2018). It is kept for reference only — `install.sh` never links
anything from here. The same snapshot is tagged `legacy-2018`.
