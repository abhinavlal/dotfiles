# Core — everything the config in home/ expects to exist.
# Install with: brew bundle --file=Brewfile   (or ./install.sh --brew)

# Shell
brew "starship"                  # prompt           (.zshrc, .config/starship.toml)
brew "zsh-autosuggestions"       # fish-style hints (.zshrc)
brew "zsh-syntax-highlighting"   # command colors   (.zshrc)

# Everyday CLI
brew "eza"                       # ls replacement   (ls/l/ll/la/lt aliases)
brew "bat"                       # cat + MANPAGER   (cat alias, fzf previews)
brew "fd"                        # find             (fzf file/dir sources)
brew "fzf"                       # ctrl+r / ctrl+t / alt+c
brew "zoxide"                    # `z <frecent dir>`
brew "gh"                        # GitHub CLI       (.config/gh)
brew "uv"                        # py / pt aliases

# Language versions
brew "nvm"                       # node versions    (.zshrc; .nvmrc)
brew "pyenv"                     # python versions  (.zshrc; .python-version)
brew "xz"                        # lzma for pyenv-built pythons
brew "bun"                       # JS runtime + package manager (.zshrc)

# Terminal
cask "ghostty"                           # .config/ghostty/config
cask "font-jetbrains-mono-nerd-font"     # prompt glyphs; without it icons render as boxes
